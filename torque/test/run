cat /proc/cpuinfo | grep "ARM926EJ-S"
if [ $? == 0 ];then
	echo "arm platform"
    export LD_LIBRARY_PATH=../lib/arm:../../cppsqlite3/lib/arm
    compath="/dev/ttymxc0"
else
	echo "x86 platform"
    export LD_LIBRARY_PATH=../lib/x86:../../cppsqlite3/lib/x86
    compath="/dev/ttyUSB0"
fi
./test
